from __future__ import annotations

from fastapi import APIRouter


router = APIRouter(prefix="/cases", tags=["cases"])


# Placeholder router to keep structure; add endpoints later and wrap with RBAC as needed.

